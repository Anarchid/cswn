// Generated by tools/make-gunship-art.mjs (2026-08-19): the FORCE FIELD's beam
// tile, 320x32 RGBA — gusanos/dark/PowerEternal's `beam.png` chevron chain with
// its red channel lifted into all three.
//
// The lift is not a restyle, it is a REQUIREMENT. The source draws the whole
// pattern in RED ONLY (g and b top out at 15), and the beam shader multiplies
// texel by the line's `color`; tinting that file green multiplies ~0 by green
// and the beam vanishes. Greyscale hands the hue back to `line.color`, where a
// green force field can ask for it.
//
// `length: 96` on the line def, so one tile spans 96 world px and the chevrons
// scroll down the beam roughly three times over its 290px reach.
{
  "frames": 1,
  "origin": [
    0,
    0
  ]
}
