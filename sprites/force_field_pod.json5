// Generated by tools/make-gunship-art.mjs (2026-08-19): the FORCE FIELD pod as
// the carrier particle draws it. One frame of 18x12, byte-identical to
// bodies/force_field_emitter.stain.png — it is the same buffer, written twice,
// so the sprite and the body can never drift apart.
//
// `origin` is the negated COM ([9, 5.15]) at whole-pixel resolution.
// `spawn_body` lands the body's ANCHOR on the carrier's position, and with no
// `spawn_offset` on the emitter that anchor IS the COM — so this is the one
// offset that makes the sprite and the body it becomes cover the same pixels,
// and the swap at tick 22 shows nothing at all.
{
  "frames": 1,
  "origin": [
    -9,
    -5
  ]
}
